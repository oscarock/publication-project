<%= render 'partials/messages' %>

<div class="d-flex justify-content-between">
  <div class="text-center col-md-12 mb-3 mt-3"><h4>Publicaciones</h4></div>
</div>
<div class="cards-grid container-fluid w-75">
  <%= render "search" %>
  <div class="row row-cols-1 row-cols-md-3">
    <% @publications.each do |publication| %>
      <div class="col">
        <div class="card border-secondary">
          <%= render publication %>
        </div>
      </div>
    <% end %>
    <div class="digg_pagination">
      <%= will_paginate @publication, :container => false, :previous_label => 'Atrás', :next_label => 'Siguiente' %>
    </div>
  </div>
</div>
